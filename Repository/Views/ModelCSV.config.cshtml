@using Seal.Model;
@{
    ReportViewTemplate Template = Model;

    //Template Name
    Template.Name = "aaa";// ReportViewTemplate.ModelCSVExcelName;

    //If true, the view can be configured for a report model
    Template.ForReportModel = true;

    //Parameters for this template
    Template.Parameters.Add(new Parameter() { Name = "csv_separator", Value = "", DisplayName = "CSV Separator", Description = "The separator used to generate the CSV file. If empty, the default value of the current culture is used." });
    Template.Parameters.Add(new Parameter() { Name = "use_display_format", DisplayName = "Use display format", BoolValue = true, Description = "If true, the display format is applied to the values displayed in the CSV file" });

    //Allowed template parent names
    Template.ParentNames = new List<string>() { "Report" };
}
