@using Seal.Model;
@{
    ReportViewTemplate Template = Model;

    //Template Name
    Template.Name = "Open HTML";

    //If for model, the Model parameter object used for the parsing is the Model instead of the Report
    Template.ForModel = false;

    //This specify to open the view in a dedicated file having the extension
	//Can be used in conjunction with Report.FinalResultFilePath to specify another file result in the template
    //Template.ExternalViewerExtension = ""; 
	
    //Parameters for this template

    //CSS

    //Allowed template parent names
    Template.ParentNames = new List<string>() { "Report", "Tab Page" };
}