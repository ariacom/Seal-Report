# Sample prompts — Schedule & Output
# One prompt per line. Blank lines and lines starting with # are ignored.

# --- Outputs ---
Create an Excel output for [report name] saved in my Personal folder
Create a PDF output for [report name] saved in C:\Reports\Daily
Change the output format of [report name] to CSV
Remove the output from [report name]

# --- Schedules ---
Schedule [report name] to run every Monday at 8am
Schedule [report name] to run every day at 7am
Schedule [report name] every Monday at 9am
Schedule [report name] on the 1st of every month at 6am
Run [report name] once tomorrow at 3pm
Delete the schedule for [report name]

# --- Review ---
Show me the outputs and schedules configured for [report name]
List all reports that have a schedule
