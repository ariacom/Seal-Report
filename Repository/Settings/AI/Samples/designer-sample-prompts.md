# Sample prompts — Report Designer
# One prompt per line. Blank lines and lines starting with # are ignored.

# --- Exploration ---
List all available data sources and their tables
What columns are in the [table] table?
Show me a sample of data from [table]
How many rows are in [table]?

# --- Create reports ---
For [source], create a report showing total sales by category
For [source], build a monthly sales trend report for this year
For [source], create a top 10 customers report ranked by revenue
For [source], make a report with a bar chart of orders by country
For [source], create a report with a pie chart of revenue by region

# --- Edit reports ---
Add a date restriction to the [report name] report
Add a bar chart to the [report name] report
Rename the [report name] report to [new name]
Move the [report name] report to the [folder] folder

